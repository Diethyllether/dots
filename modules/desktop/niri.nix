{ configs, pkgs, inputs, ... }:

{
  programs.niri = {
    enable = true;
    settings = {
      binds = {
        
      };
      switch-events = {
        
      };

      spawn-at-startup = {
        
      };

      workspaces = {
        
      };

      overview = {
        
      };

      input = {
        
      };

      outputs = {
        
      };

      layout = {
        
      };

      animations = {
        
      };

      environment = {
        
      };

      window-rules = {
        
      };

      layer-rules = {
        
      };
    };
  };
}
